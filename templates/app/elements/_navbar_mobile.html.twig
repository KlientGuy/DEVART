{% trans_default_domain 'layout' %}
<header class="d-flex px-3 rounded-3 justify-content-between {{ app.request.get('_route') != 'app_index' ? 'w-100' : 'p-3'}}">

    <a href="{{ path('app_index') }}" class="ms-0 nav-btn justify-center {{ app.request.get('_route') != 'app_index' ? '' : 'd-none'}}" style="flex: 0">
        <img src="{{ asset('assets/svg/logo.svg') }}" alt="polska" width="80">
    </a>
    <div class="burger-button mobile">
        <span></span>
        <span></span>
        <span></span>
    </div>
</header>
